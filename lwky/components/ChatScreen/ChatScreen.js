import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';

const ChatScreen = ({navigation}) => {
  return (
    <View>
      <Text>Chat Screen</Text>
    </View>
  );
};

export default ChatScreen;
